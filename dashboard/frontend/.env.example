# Example environment variables for the Vite frontend
# Copy this file to .env and replace the values with your local setup.

# The full URL of the backend API.
# This must be accessible from the device running the frontend.
# Use your machine's local IP address to allow access from other devices on the same network.
VITE_API_URL=http://192.168.1.100:8000
